# CrossBridge AI · 项目主档案（PROJECT_CONTEXT.md）

> 本文件是 **CrossBridge AI** 项目的唯一主档案（Single Source of Truth）。
> 用途：任何新会话、新协作者、AI 助手在接续本项目前，**先读本文件**，无需重新问询背景。
> 写法约定：**已确认事实**（代码/线上/文件中可验证）与 **方案规划**（计划中、未实现）严格分开；
> 无法核实的内容一律标注 **【待确认】**，**不臆造任何数据**。
> 生成时间：2026-09-04 ｜ 对应代码版本：git `b3e2302`（main 分支）

---

## 一、项目基本信息

| 项目 | 内容 |
|---|---|
| 项目名称（中文） | 跨境电商跨文化沟通风险智能检测助手 |
| 项目名称（英文） | CrossBridge AI |
| 赛道 | 全国大学生新文科实践创新大赛 · 实践创新报告类 |
| 高校 | 哈尔滨商业大学 |
| 团队负责人 | 商霄汉（统筹 + 全栈开发） |
| 团队成员 | 张熙迎（专业内容 + 跨文化研究）、姜语涵（场景分析 + 数据整理） |
| 指导教师 | 陈璐（哈尔滨商业大学外语学院商务英语教研室，讲师；研究方向：商务英语、跨境电子商务） |
| 实践形式 | 软硬件开发（**【待确认】**：报告中的勾选项需与最终提交版一致） |
| 组别 | **【待确认】**（报告勾选项未填写） |
| 填报日期 | 2026 年 9 月 4 日 |
| 代码仓库 | https://github.com/Snape001S/CrossBridge-AI （main 分支） |
| 线上地址 | https://crossbridge-ai.onrender.com/ |
| 开源许可 | MIT |
| 当前版本 | `1.0.0`（`GET /api/health` 返回） |

**一句话定位**：一个"AI + 商务英语 + 跨文化交际"的新文科实践创新项目，
面向跨境电商与国际商务沟通场景，检测沟通文本中的 **语言 / 商务礼仪 / 跨文化** 风险，
输出 **风险等级 + 问题分析 + 优化表达 + 文化解释**。

---

## 二、100 字摘要（对外最短版）

CrossBridge AI 是面向跨境电商与国际商务沟通的跨文化风险检测助手。
用户输入英文沟通文本，系统从语言准确性、商务礼貌、文化适配、表达清晰、合作友好五个维度打分，
判定风险等级，指出具体问题，给出优化表达与文化解释。项目融合商务英语专业理论与 AI 技术，
覆盖邮件、谈判、投诉、售后等场景，已上线可访问的完整 Demo。

---

## 三、500 字项目上下文（给 AI 助手 / 评委的背景说明）

**是什么**：CrossBridge AI 是一个 Web 应用。用户在浏览器输入一段英文（或中英混合）商务沟通文本，
选择沟通场景、目标文化与业务场景，点击分析后，系统在数秒内返回一份"沟通风险报告"：
五维评分、总分（0–100）、风险等级、逐条问题分析、AI 推理过程、优化后的表达、跨文化策略建议、同场景案例参考。

**不是什么**（这是本项目的关键边界，任何改版都不得越过）：
不是英语学习软件（不做背单词、语法课、题库）；不是翻译软件（不做逐句互译）；
不是普通 AI 写作助手（不做通用润色、不做营销文案生成）。
它的唯一核心是 **"跨文化沟通风险的识别与解释"**——即"这句话在对方的文化语境里为什么有风险、风险有多大、应该怎么说"。

**为什么要做**：跨境电商与外贸一线的真实痛点不是"英语不好"，而是
"英语看着没错、但让对方不舒服／丢面子／觉得失礼，导致订单流失、谈判破裂、客户投诉升级"。
传统的英语能力训练解决不了这一层，因为它是 **语用与文化层面** 的问题。
项目把商务英语专业的跨文化交际理论（霍尔高低语境、霍夫斯泰德文化维度、面子理论、礼貌原则、语步结构）
转化为可计算、可复用的检测规则与 AI 提示词。

**怎么做**：后端 FastAPI，采用 **大模型 + 规则引擎双引擎**：
配置了 API Key 时调用大模型（DeepSeek，结构化 JSON 输出）；
无 Key 或调用失败时自动降级到基于 52 个真实案例知识池的规则引擎，保证演示永不"白屏"。
评分采用确定性算法（五维加权求和），避免大模型打分漂移。

**做到哪一步了**：6 个页面、6 个 API 全部可用并已部署上线；
案例库 30 条 + 场景测试用例 22 条 + 优化对比组 8 组；
已用 53 份用户反馈完成一轮实践验证。**尚未实现**：账号体系、历史记录、数据库持久化、
图片/语音多模态输入、批量报告导出。

---

## 四、项目定位与边界

### 4.1 做什么（核心价值）

1. **风险检测**：识别语言问题、商务礼仪问题、跨文化语用问题。
2. **风险分级**：五维评分 → 总分 0–100 → 低风险 / 中风险 / 中高风险 / 高风险。
3. **问题分析**：逐条指出问题所在、为什么是问题、涉及哪一维度。
4. **优化表达**：给出可直接使用的改写版本 + 改写策略说明。
5. **文化解释**：解释目标文化语境下的沟通规则与理论依据。

### 4.2 不做什么（反面清单，防止定位被稀释）

- ❌ 不做英语教学（不提供单词、语法、听力、题库、课程）
- ❌ 不做翻译工具（不提供双语互译、不主推"翻译得对不对"）
- ❌ 不做通用写作助手（不做论文/文案/营销/代码写作）
- ❌ 不做社交通讯工具（不接入 WhatsApp/微信等做实时聊天）
- ❌ 不做"AI 评测不可解释"的黑箱（每条分数必须给出依据）

> **维护规则**：后续任何功能增补，若与 4.2 冲突，一律不做；若有助于 4.1，优先做。

---

## 五、要解决的现实问题（痛点链条）

| 层级 | 痛点 | 现有方案为何不够 |
|---|---|---|
| 语言层 | 语法没错但语气生硬、指令式表达 | 语法检查工具只查对错，不查语气与关系 |
| 商务礼仪层 | 直接拒绝、直接催款、直接否定对方方案 | 教材讲原则，缺少针对具体句子的即时反馈 |
| 跨文化层 | 高低语境差异、面子威胁、直白与含蓄错位 | 翻译工具只转换语言，不转换语用策略 |
| 反馈层 | 学生/从业者写完邮件没人给专业反馈 | 教师人工批改成本高、时效差、无法覆盖全部场景 |
| 验证层 | 培训效果难以量化 | 缺少可量化的沟通风险指标 |

**项目主张**：把"跨文化交际能力"从"靠经验、靠悟性"变成
**"可检测、可分级、可解释、可训练"** 的过程。

---

## 六、目标用户与使用场景

### 6.1 目标用户

1. **主用户**：跨境电商运营 / 外贸业务员 / 国际商务专业学生（在校与实习阶段）
2. **次用户**：商务英语教师（作为教学辅助与案例来源）
3. **潜在用户**：中小企业出海团队（缺少专职跨文化培训）

### 6.2 已实现的沟通场景（8 类 `scene`）

商务邮件 `email`、商务谈判 `negotiation`、国际会议 `meeting`、客户沟通 `customer`、
日常交流 `daily`、跨境电商 `ecommerce`、商务接待 `reception`、一般沟通 `general`

### 6.3 已实现的目标文化（6 项 `culture`）

自动识别 `auto`、美国 `us`、欧洲 `europe`、日本 `japan`、东南亚 `seasia`、中东 `mideast`

### 6.4 已实现的业务场景（7 项 `business_scene`）

自动识别 `auto`、产品询价 `inquiry`、报价回复 `quotation`、商务谈判 `negotiation`、
客户投诉 `complaint`、合同沟通 `contract`、售后服务 `aftersales`

---

## 七、产品形态与功能全景

**形态**：单页式多页面 Web 应用（零构建：原生 HTML + CSS + JS，无 npm / 无 node 依赖）。
后端直接托管前端静态资源，同源部署，无跨域配置负担。

### 7.1 六个页面

| 页面 | 文件 | 作用 |
|---|---|---|
| 首页 | `frontend/index.html` | 定位展示 + 立即分析入口 + 快速体验案例 + 场景中心 |
| 风险报告页 | `frontend/report.html` | 分析结果完整呈现（评分 / 问题 / 优化 / 文化解释 / 案例） |
| 案例库 | `frontend/cases.html` | 30 条真实跨文化风险案例，支持场景筛选 + 来源筛选 + 搜索 |
| 沟通诊断 | `frontend/diagnosis.html` | 多条文本 → 聚合沟通风险画像（优势 / 短板 / 建议） |
| 优化对比 | `frontend/comparison.html` | 8 组"优化前 vs 优化后"双栏对比 + 双雷达图 + 分数变化 |
| 模型说明 | `frontend/model.html` | CCRM 六层模型的静态讲解页（评委理解评分依据用） |

### 7.2 六个后端接口

`GET /api/health`、`POST /api/analyze`、`GET /api/cases`、
`POST /api/diagnosis`、`GET /api/comparisons`、`GET /api/scene-cases`

（详见第十三章）

---

## 八、系统架构与技术栈

```
浏览器（原生 HTML/CSS/JS，零构建）
        │  同源 fetch
        ▼
FastAPI (backend/main.py)
        ├── 路由层：6 个 API
        ├── 静态托管：StaticFiles(html=True) → frontend/
        ├── 分析编排：analyzer.analyze()  ← 双引擎调度 + 降级
        │        ├── 大模型通道：providers.chat() → DeepSeek / SiliconFlow / Ollama
        │        └── 规则引擎：mock_analyzer.analyze_rule()（52 案例知识池）
        ├── 数据访问：cases.py / aggregator.py
        └── 数据文件：data/*.json（4 个 JSON，无数据库）
```

| 层 | 技术 | 说明 |
|---|---|---|
| 后端 | Python 3.11 + FastAPI + Uvicorn + Pydantic | `backend/requirements.txt` |
| 前端 | 原生 HTML/CSS/JavaScript（ES5 风格，无框架） | 不用 npm / 构建工具，双击即可改 |
| 大模型 | DeepSeek（OpenAI 兼容协议，`urllib` 直连） | 另兼容 SiliconFlow、Ollama |
| 数据 | 4 个 JSON 文件 | **无数据库**（无 SQLite / 无 MySQL 文件） |
| 部署 | Docker + Render 免费 Web Service | `Dockerfile` / `render.yaml` |
| 版本控制 | Git + GitHub | 仓库 `Snape001S/CrossBridge-AI` |

**技术选型理由**：项目需在比赛现场与免费云环境稳定演示，
零构建前端消除依赖安装失败风险；双引擎消除 API Key 失效/超时导致演示中断的风险；
JSON 数据文件让案例可被非技术人员（专业内容成员）直接编辑。

---

## 九、AI 双引擎工作流（核心技术）

### 9.1 调度逻辑

`backend/config.py`：`AI_PROVIDER` 默认 `auto`，`resolve_provider()` 依次判断：

```
auto → 有 DEEPSEEK_API_KEY ? deepseek
     → 有 SILICONFLOW_API_KEY ? siliconflow
     → 否则 mock（规则引擎）
```

### 9.2 大模型通道（当前线上实际生效）

一次完整分析 = **2 次串行 LLM 调用**：

| 调用 | 内容 | 产出 |
|---|---|---|
| call1 | 场景识别 + 问题清单 + 风险推理 | `scene` / `issues[]` / `reasoning` |
| call2 | 五维打分 + 优化表达 + 跨文化策略 | `scores` / `fixes[]` / `dimension_reasons` / `summary` |

- 结构化输出：要求模型返回严格 JSON，后端用 `parse_json()` 容错解析（剥离 markdown 代码块等）。
- 重试：单步失败最多重试 `MAX_LLM_RETRY=1` 次（异常/解析失败触发）。
- HTTP 层重试：`providers._post_json()` 对 `429 / 500 / 502 / 503 / 504` 及网络异常重试，
  退避 1.5s → 3s。
- 超时：`REQUEST_TIMEOUT=90` 秒。
- 模型：`DEEPSEEK_MODEL` 代码默认 `deepseek-chat`
  （**【待确认】**：Render 环境变量是否覆盖为其他模型，需登录 Render 后台核对）。
- 密钥：仅存在于 Render 环境变量 `DEEPSEEK_API_KEY`，**不在前端、不在仓库**；
  `/api/health` 只暴露布尔值 `deepseek_configured`。

### 9.3 规则引擎通道（降级 / 无 Key 模式）

`backend/mock_analyzer.py`，知识池 `MATCH_POOL = 30 条风险案例 + 22 条场景用例 = 52 条`：

1. **关键词匹配**：`KEYWORD_CATEGORY` + 全文关键词扫描（含 `must pay` / `pay us` /
   `not interested` / `too expensive` / `another company` / `choose another` 等已收紧的触发词）
2. **通用模式匹配**：`GENERIC_PATTERNS`（如命令式祈使句、全大写、生硬否定）
3. **文化因素**：`CULTURE_FACTORS` 按目标文化补充解释
4. **评分推导**：命中问题类型 → 扣分 → 五维分数 → 加权总分
5. **策略生成**：`_build_strategies()` 按 `relationship` / `negotiation` / `assertive` 三类模板输出
6. 输出标注 `mode="rule"`，前端显示"规则引擎模式"

### 9.4 降级链路（保命机制）

```
analyze(text, scene, culture, business_scene)
  ├── 若 provider == mock → analyze_rule()
  └── 否则 → _llm_analyze()（call1 + call2，带重试）
            └── 任何异常 / 解析失败 / 超时
                 → analyze_rule()，并在 provider 字段标注 "deepseek(降级)"
```

**设计意图**：比赛现场网络异常时，页面仍能给出完整报告，不出现空白或报错。

### 9.5 AI 调用规模与性能实测

| 操作 | LLM 调用次数 | 备注 |
|---|---|---|
| 单次分析 `/api/analyze` | 2（异常时最多 4） | 串行 |
| 沟通诊断 `/api/diagnosis` | 文本数 × 2（默认 5 条 → 10 次） | **串行，无并发** |
| 优化对比 `/api/comparisons` | 8 组 × 2 = 16 次 | 首次计算后进程内缓存 `_COMPARE_CACHE` |
| 案例库 / 场景用例 | 0 | 纯 JSON 读取 |

**实测（线上，DeepSeek，2026-09-04 实测数据）**：

| 接口 | 实测耗时 | 说明 |
|---|---|---|
| `GET /api/health` | **1.2 – 1.6s** | 正常 |
| `POST /api/analyze` | **9.2s** | `mode=ai`、`provider=deepseek`、`total=44`、`issues=3`、`strategies=3`、`related_cases=2` |
| `GET /api/comparisons`（首次，冷缓存） | **118.0s** | 16 次串行 LLM 调用，返回 `total=8`、11128 字节 |
| `GET /api/comparisons`（二次，命中缓存） | **1.0s** | 进程内 `_COMPARE_CACHE` 生效 |
| `POST /api/diagnosis`（5 条文本） | **39.1s** | 10 次串行 LLM 调用，返回 `total=43` |

- ⚠️ **P0 级问题（已实测确认，非推测）**：前端 `api.js` 超时为
  **分析 25s / 诊断 30s / 对比 15s / 案例 10s**。
  实测对比接口首次需 **118s**（超时 15s 的 **约 8 倍**）、诊断接口需 **39.1s**（超过 30s），
  因此 **只要服务重启或休眠唤醒导致缓存失效，对比页与诊断页必然触发前端超时**，
  回落 `mockdata.js` 内置数据并显示离线提示。
  → 详见第二十章 P0 与第二十一章修复方案。
- 另观察：首次测量时两个重接口均在 **约 30.9s** 被服务端断开
  （`Remote end closed connection without response`），复测（先请求 health 预热连接）后成功返回，
  疑与免费实例冷启动/连接层切断有关，**【待确认】** 成因，但已构成现场演示风险。
- 全流程**无并发**：所有 `analyze()` 调用按顺序执行，这是上述耗时的主要成因。

---

## 十、五维评估模型与风险分级（确定性算法）

### 10.1 五个维度（`backend/models.py` → `DIM_WEIGHTS`）

| 维度代码 | 中文（前端展示） | 英文 | 权重 | 满分贡献 |
|---|---|---|---|---|
| `language_accuracy` | 语言准确性 | Language Accuracy | **3.0** | 30 |
| `business_politeness` | 商务礼貌 | Business Politeness | **2.0** | 20 |
| `cultural_adaptability` | 文化适配 | Cultural Adaptability | **2.0** | 20 |
| `communication_clarity` | 表达清晰 | Communication Clarity | **2.0** | 20 |
| `relationship_friendliness` | 合作友好 | Relationship Friendliness | **1.0** | 10 |

- 每维 0–10 分（Pydantic 强校验 `ge=0, le=10`）。
- 总分 = `round(Σ 维度分 × 权重)`，范围 0–100，**由后端计算**，
  避免大模型直接给总分造成漂移。

### 10.2 风险分级（`RISK_LEVELS`）

| 总分区间 | 风险代码 | 中文标签 | 英文 |
|---|---|---|---|
| ≥ 85 | `low` | 低风险 | Low Risk |
| 70 – 84 | `medium_low` | 中风险 | Medium Risk |
| 50 – 69 | `medium_high` | 中高风险 | Medium-High Risk |
| < 50 | `high` | 高风险 | High Risk |

### 10.3 理论支撑（跨文化交际）

霍尔高低语境理论（Hall）、霍夫斯泰德文化维度（Hofstede）、面子理论（Brown & Levinson）、
语用学礼貌原则（Leech）、商务语篇语步结构（Move Structure）。
→ 在 `model.html` 中以 CCRM 六层模型对外呈现。

### 10.4 问题类型与维度归并（`render.js`）

- 问题类型：`language`（语言问题）、`etiquette`（商务礼仪）、`culture`（跨文化）等
- 归并展示：语言准确性 → "语言准确性"；文化适配 + 合作友好 → "文化适配与合作友好"

---

## 十一、Prompt 与结构化输出契约

`backend/prompts.py`：

| 组成 | 作用 |
|---|---|
| `SYSTEM_PROMPT` | 角色设定：跨文化商务沟通专家，只做风险检测与优化，不做翻译教学 |
| `CALL1_USER_TEMPLATE` | 输入文本 + 场景 + 目标文化 + 业务场景 → 要求输出场景识别、问题清单、风险推理 |
| `CALL2_USER_TEMPLATE` | 基于问题清单 → 要求输出五维评分、各维理由、总评、优化表达（含策略键） |
| `build_messages_call1/2()` | 组装消息，注入文化提示（`culture`）与业务场景提示（`business_scene`） |

**策略键（`strategies`）**：`relationship`（关系维护）/ `negotiation`（谈判策略）/ `assertive`（坚定表达）

**输出契约（`AnalysisResult`）**：`text, scene, scene_label, scores, total, risk_level, risk_label,
summary, issues[], fixes[](含 strategies[]), dimension_reasons{}, related_cases[], provider,
mode, culture, culture_label, business_scene, business_scene_label, reasoning{...}`

**失败处理**：JSON 解析失败 → 记录并重试；重试仍失败 → 降级规则引擎（标注"降级"）。

---

## 十二、数据资产（`data/`，4 个 JSON，无数据库）

| 文件 | 条目数 | 结构 |
|---|---|---|
| `cross_culture_cases.json` | **30** | 场景分布：邮件 10 / 谈判 8 / 会议 5 / 客户 5 / 日常 2；字段含 `problem_type`、`source_type`、`match_keywords`、`issue_type`、`tags` |
| `scene_test_cases.json` | **22** | 场景分布：邮件催办 4 / 价格谈判 4 / 方案拒绝 4 / 客户投诉 4 / 商务邀约 4 / 会议探询 2 |
| `comparison_pairs.json` | **8** | 每组含优化前 / 优化后文本 + 说明，用于对比页 |
| `case_resolutions.json` | **30** | 与 30 条案例一一对应的"解决方案"文本，由 `cases.get_all()` 挂载为 `resolution` |

**案例总数：30（风险案例）+ 22（场景用例）= 52 条**，即规则引擎知识池规模。
所有案例**来源标注**（`source_type`）区分真实案例转写与教学构造案例。

**数据持久化现状**：用户输入文本、生成的报告、账号信息 **均不落库**。
报告页依靠 `sessionStorage`（键 `cb_report_v1` / `cb_mode_v1`）在同一次浏览会话内传递结果；
刷新或直达报告页会走内置示例数据并显示离线提示。

---

## 十三、接口清单（API 规范）

| # | 方法 | 路径 | 入参 | 出参 | AI | 状态 |
|---|---|---|---|---|---|---|
| 1 | GET | `/api/health` | — | `status / service / version / ai_provider / deepseek_configured / mode_note` | 无 | ✅ 线上正常 |
| 2 | POST | `/api/analyze` | `{text, scene, culture, business_scene}` | `AnalysisResult` | 2 次 | ✅ 线上正常（约 9.2s） |
| 3 | GET | `/api/cases` | `?scenario=` | 案例数组（含 `resolution`） | 无 | ✅ |
| 4 | POST | `/api/diagnosis` | `{texts: [1..20]}` | 聚合画像（总分 / 雷达 / 优势 / 短板 / 建议） | 文本数×2 | ⚠️ 实测 39.1s（5 条）> 前端 30s 超时 |
| 5 | GET | `/api/comparisons` | `?force_refresh=` | 8 组对比（前后总分、变化值、双雷达） | 16 次（含缓存） | ⚠️ 实测首次 118s > 前端 15s 超时；缓存后 1.0s |
| 6 | GET | `/api/scene-cases` | — | 22 条场景用例 | 无 | ✅ |

- 线上健康检查实测：`{"status":"ok","service":"CrossBridge AI","version":"1.0.0",
  "ai_provider":"deepseek","deepseek_configured":true}` → **DeepSeek 已实际启用**。
- CORS：允许全部来源（`allow_origins=["*"]`），便于本地与多环境调试。
- 对比页语义：`降低值 = 优化后总分 − 优化前总分`，前端显示"评分提升 +N · 风险等级降低"。

---

## 十四、前端页面与交互实现

### 14.1 资源结构

```
frontend/
├── index.html / report.html / cases.html / diagnosis.html / comparison.html / model.html
└── assets/
    ├── css/style.css          （全站唯一样式表，含移动端媒体查询）
    └── js/
        ├── api.js             （接口封装 + 超时 + 离线兜底）
        ├── render.js          （渲染器：环形图/雷达图/仪表/问题卡/优化卡/案例卡…）
        ├── mockdata.js        （离线内置数据与示例报告）
        └── app.js             （页面初始化、场景选择、交互、动画、筛选）
```

### 14.2 首页结构（信息层级：少宣传、多使用）

1. 导航栏（含移动端汉堡菜单 `#navToggle` / `.nav-open`）
2. 首屏：标题 + 分析输入卡片（场景胶囊 / 文化胶囊 / 业务场景胶囊 + 文本框 + 开始分析按钮）
3. 快速体验（4 个一键示例：`price` 价格谈判 / `complaint` 客户投诉 / `refund` 退款售后 / `competitive` 竞争对手）
4. 流程条（输出什么 → 怎么判断 → 能得到什么）
5. 能力/价值区（核心统计）→ 功能网格 → 场景中心（5 个跨境电商场景卡）→ 关于/收尾

**移动端首屏压缩**：375px 下"开始分析"按钮顶部位置压缩至约 652px（< 667px 可视高度），
用户无需滚动即可看到主操作。

### 14.3 报告页信息结构

评分环 + 风险等级徽章 + 总评 → 风险仪表 → 五维雷达 + 维度条 →
STEP① 风险分析（问题卡 + AI 分析过程/推理）→ STEP② 评分依据（判断依据）→
STEP③ 同场景案例参考 → STEP④ 优化表达（改写 + 策略 + 量化对比）→ 应用价值 → AI 使用说明。

### 14.4 移动端适配（已完成一轮根因修复）

- 断点：900px / 768px / 480px；≤768px 显示汉堡导航（桌面端 `display:none` 已校验）。
- **根因修复记录**：基础栅格规则（`.core-stats` / `.feature-grid` / `.value-points` / `.model-dim-grid`）
  原先写在媒体查询之后，同优先级下后者覆盖前者，导致 375px 仍为 3 列并横向溢出
  （实测 `core-stat` 右边界 477px > 375px）。
  修复方式：在 `style.css` **末尾追加最终覆盖媒体查询** + 对 grid/flex 子元素补 `min-width: 0`
  + `html, body { overflow-x: hidden; }` 保护。
- 验证尺寸：375 / 390 / 414 / 768（移动端）与 1024 / 1280 / 1440（桌面端）。

---

## 十五、部署与运维

| 项 | 内容 |
|---|---|
| 容器 | `Dockerfile`：基础镜像 `python:3.11-slim`，启动 `uvicorn main:app --host 0.0.0.0 --port ${PORT:-7860}` |
| 忽略 | `.dockerignore` |
| 云平台 | Render 免费 Web Service（Docker 运行时） |
| 蓝图 | `render.yaml`（服务名 `crossbridge-ai-demo`） |
| 自动部署 | GitHub `main` 分支推送 → Render 自动构建部署 |
| 环境变量 | `DEEPSEEK_API_KEY`（Render 后台配置，**【待确认】** 完整变量清单需登录后台核对） |
| 已知平台限制 | Render 免费实例会休眠，冷启动首请求较慢；HF Docker SDK 已改为付费，故未采用 |

**推送流程（本地约定）**：仓库已配置本地代理 `http.proxy / https.proxy = http://127.0.0.1:7897`；
认证使用 PAT（临时写入 `_token.txt` 后以 token-in-URL 方式 push，完成后重置 URL 并删除文件）。
**安全约定**：密钥/PAT 绝不写入仓库、绝不写入前端代码。

---

## 十六、测试与质量保障

### 16.1 本地脚本（`scripts/`，已 `.gitignore`，**不在 GitHub 仓库中**）

| 脚本 | 作用 |
|---|---|
| `acceptance_test.py` | 验收测试 **21 项检查**（接口、评分、风险等级、降级、页面） |
| `online_check.py` | 线上接口连通性检查 |
| `prelaunch_check.py` | 上线前检查 |
| `css_audit*.py` / `check_*.py` | CSS 结构审计、布局度量 |
| `perf_check.py` / `perf_check2.py` / `edge_case_check.py` | 线上重接口耗时实测、代理干扰排查、边界文本复测（本轮新增） |
| `scan_desktop_docs.py` | 桌面比赛文档内容扫描（字数、数据标记、版本判定） |
| `read_report_docx.py` / `count_report_words.py` / `fix_report_docx.py` / `apply_refine.py` | 比赛报告 docx 读取、字数统计、修订、精修 |
| `master_check.py` | 主档案事实核对 |

⚠️ **风险提示**：`scripts/` 被 `.gitignore` 排除，故验收脚本**仅存在于本地**，
换机/协作时不可复现测试。**【待确认】**：是否将测试脚本纳入版本管理（建议纳入或另建测试仓库）。

### 16.2 已验证项目（已确认）

- 6 个页面可访问；6 个接口线上可用；健康检查字段完整。
- DeepSeek 通道贯通：线上 `analyze` 返回 `mode=ai`、`provider=deepseek`。
- 降级通道有效：无 Key / 异常时返回 `mode=rule` 且 provider 标注"降级"。
- 移动端 375 / 390 / 414 / 768 无横向溢出，汉堡导航正常。
- 桌面端 1024 / 1280 / 1440 栅格正常；容器最大宽度 1200px。
- 首页信息层级与首屏压缩已复核。

### 16.3 验证方法说明

因本机内置浏览器工具不可用（`browser host exited code=0`），
页面结构与布局度量采用 **headless Chrome**（`--dump-dom` / `--screenshot` / `--window-size`）
配合 iframe 度量页完成；中文与正则相关检查统一移入 Python 脚本写 UTF-8 文件，
以规避 PowerShell 的中文编码问题。

---

## 十七、比赛材料与文档资产

### 17.1 项目内 `docs/`

`01_产品原型与UI设计方案.md`、`02_比赛升级方案.md/.html`、`上线指南.md`、
`存档-移动端适配.md`、`部署指南.md`、`部署说明.md`、`项目开发方案书.md`、`screenshots/`（v1–v8 与 375px 系列截图）

### 17.2 桌面比赛文档（`C:\Users\sjhjm\Desktop\`，2026-09-04 实际扫描结果）

统计口径：中文字数 = 解压 `word/document.xml` 后统计的 CJK 汉字数（不含标点、数字、英文）。

| 文件（桌面现状） | 中文字数 | 数据标记 | 判定 |
|---|---|---|---|
| `附件1大学生新文科实践创新大赛实践创新告.docx` | 7507 | `53份`×4，92.5%/94.3%/98.1%/84.9% 各×3；**无** 26份/92.3% | ✅ **全量修订版**（正式提交首选候选；注意文件名漏了"报"字，提交前应改名为"…实践创新报告.docx"） |
| `跨境电商跨文化沟通风险智能检测助手--实践创新报告 .docx` | 5587 | `53份`×5，四项百分比各×3；**无**旧数据 | ✅ **精修版**（压缩宣传性表述，5-2 保留原稿） |
| `附件1大学生新文科实践创新大赛实践创新报告(2)(4).docx` | 7892 | `53份`×2、`26份`×1、`92.5%`×1、`92.3%`×4 | ⚠️→📦 **已归档**（新旧数据混杂，最危险的一个） |
| `附件1大学生新文科实践创新大赛实践创新报告(2).docx` | 9477 | `26份`×3、`92.3%`×6 | ⚠️→📦 **已归档**（全旧数据；篇幅长于现行版，含已删减文字，已保留备查） |
| `新建 DOCX 文档 (2).docx` | 4237 | `26份`×3、`92.3%`×2 | ⚠️→📦 **已归档**（含专业代码行 `JKJY，SYLL，ZRKX`） |
| `新建 DOCX 文档.docx` | 455 | — | 教师/专家《指正》意见（首句要求与商务英语教学结合） |
| `CrossBridge-AI_项目开发方案书.docx` | 8440 | — | 项目开发方案书（对外材料） |
| `诚信签名.docx` | 335 | — | 大学生新文科实践创新大赛诚信承诺书 |

**📦 归档动作（2026-09-04 已执行）**：上表 3 个含旧口径的文件已**移动**（非删除，可随时恢复）至
`C:\Users\sjhjm\Desktop\报告旧版本存档（含26份旧数据-请勿提交）\`，
同目录附 `说明-请先阅读.txt`（说明旧口径 26 份为何不可提交、正确数据口径、以及正式版是哪两个文件）。
归档后 Python 严格复核：**桌面根目录 10 个 docx 中，含旧口径标记的 = 0**；
两个正式版仅出现 `53份` 与 92.5%/94.3%/98.1%/84.9%。

**报告分节字数（修订版 / 精修版，脚本统计）**：
2-1 553/419、2-2 882/479、3-1 395/395、3-2 1538/743、3-3 1301/533、4-1 498/498、4-2 905/905、5-1 918/626、5-2 733/733

**数据一致性修正**：报告 2-2 曾写"26 份"、3-2 写"53 份"，且百分比按 26 份样本计算；
确认 **唯一真实轮次为 53 份**，已全文统一为 53 份 与
92.5% / 94.3% / 98.1% / 84.9%，并清除残留的 `26份`、`92.3%`
（桌面旧数据文件已于 2026-09-04 归档，见上表 📦 行；现桌面根目录无旧口径数据。）

**PPT**：**【待确认】** 项目目录与桌面均未发现任何 `.pptx` 文件 —— 答辩 PPT **尚未制作**。

---

## 十八、团队与分工

| 成员 | 角色 | 职责 |
|---|---|---|
| 商霄汉 | 负责人 | 项目统筹、技术架构、前后端开发、部署与运维 |
| 张熙迎 | 专业内容 | 跨文化交际理论梳理、案例专业性与文化解释审核 |
| 姜语涵 | 场景分析 | 商务场景拆解、案例收集与数据整理、用户反馈回收 |
| 陈璐 | 指导教师 | 商务英语与跨境电商方向指导（哈尔滨商业大学外语学院商务英语教研室，讲师） |

---

## 十九、实践验证数据（唯一一轮，53 份）

| 指标 | 结果 |
|---|---|
| 有效反馈样本 | **53 份** |
| 能帮助发现潜在沟通问题 | **92.5%** |
| 认为优化建议有参考价值 | **94.3%** |
| 认为跨文化分析提升表达针对性 | **98.1%** |
| 认为可应用于实际商务场景 | **84.9%** |

**表述要求（重要）**：上述数据来自 **一轮 53 份** 的试用反馈，
**不得** 表述为"大规模市场调研"或"广泛验证"；
除上表四项外，**没有任何其他已验证数据**。

---

## 二十、当前已知问题与限制（诚实清单）

### 20.1 P0（影响比赛演示，需优先处理）

1. **【已实测确认】诊断页/对比页在 DeepSeek 模式下必然超时兜底**
   - 前端超时：分析 25s、诊断 30s、对比 15s（`api.js`）。
   - 线上实测：对比接口首次（冷缓存）**118.0s**、诊断接口（5 条文本）**39.1s**，
     均超过对应前端超时 → 页面回落 `mockdata.js` 内置数据并显示离线提示。
   - 触发条件：**服务重启、休眠唤醒、缓存失效后的首次访问**（对比缓存仅存在于进程内存）。
   - 成因：全部 LLM 调用**串行执行**（对比 16 次、诊断 10 次），单次调用约 7–9s。
   - 影响：评委若先点开"优化对比"或"沟通诊断"，会看到兜底数据与离线提示，
     与本项目"已上线可用"的表述冲突，**必须在比赛演示前修复**（修复方案见第二十一章）。
2. **【已观察】重接口存在约 30s 被服务端断连的现象**
   首次测量时对比与诊断接口均在约 30.9s 返回 `Remote end closed connection without response`，
   复测成功。疑似免费实例冷启动/连接层切断，**【待确认】** 成因，但同属 P0 演示风险。

### 20.2 P1（影响体验与可信度）

1. 直达/刷新报告页无会话数据时显示离线提示，易被误读为"系统故障"。
2. **【已复测，风险解除】规则引擎池外文本在 DeepSeek 模式下不再"未检出"**
   2026-09-04 线上复测（`scene=ecommerce`、`culture=us`、`business_scene=aftersales`）：
   - `Just give me a refund.` → `mode=ai`、`total=57`、**中高风险**、问题 2 条（etiquette / language）
   - `Your offer is not competitive.` → `mode=ai`、`total=47`、**高风险**、问题 3 条（etiquette / culture / language）
   - `Just give me a refund. I don't want to discuss it anymore.` → `total=54`、中高风险、问题 3 条
   → 规则引擎对池外文本可能漏检的问题，在 DeepSeek 模式下不存在；但**规则引擎（无 Key 演示）仍有此局限**。
3. **【已观察】用户选择的场景可能被模型覆盖**
   `analyzer._llm_analyze()` 中 `scene_final = data1.get("scene", scene)`：
   模型在 call1 识别的场景**优先于**用户在前端选择的场景
   （上例用户选 `ecommerce`，返回 `scene=customer`）。
   **【待确认】** 是否有意为之；若为演示，需注意报告页场景标签可能与用户所选不一致。
4. `scripts/` 未纳入版本管理，验收脚本无法随仓库复现。
5. README 的 `.html` / `.txt` 与 `打开Demo.bat` 亦被 `.gitignore` 排除，仓库内仅有 `README.md`。

### 20.3 P2（能力边界，非缺陷）

1. 无账号体系、无历史记录、无数据库持久化（有意为之，降低部署与合规复杂度）。
2. 无图片/语音/多模态输入；无批量导出报告（PDF/Word）能力。
3. 无并发处理，接口在人多时排队变慢。
4. 目标文化目前为 6 个预设项，未覆盖全部国别；`auto` 依赖模型判断。
5. Render 免费实例休眠导致冷启动慢。

---

## 二十一、路线图

### 已实现（v1.0.0，已上线）

- 6 页面 + 6 接口全链路可用，零构建前端，Docker + Render 部署
- 双引擎（DeepSeek + 52 案例规则引擎）与自动降级
- 五维确定性评分 + 四级风险分类 + AI 推理过程展示
- 30 条风险案例 + 22 条场景用例 + 8 组优化对比 + 30 条解决方案
- 移动端响应式适配与首页信息层级重构
- 53 份用户反馈的一轮实践验证

### 近期规划（尚未实现）

- **修复 P0（演示前必做）**：把诊断/对比接口的串行 LLM 调用改为**并发**（线程池/`asyncio.gather`），
  并在服务启动时**预热对比缓存**；同步上调前端超时（对比 15s → 180s、诊断 30s → 120s）
  并补充加载进度提示；必要时对该两页改用规则引擎快路径。
  目标：对比页首次可接受（≤60s 并显示进度）、诊断页 ≤30s 内返回真实结果，不再回落兜底数据。
- 报告页直达时提供"输入自己的文本"引导，而非仅显示离线提示
- 引入对话式/截图式输入（OCR）**【规划中，未实现】**
- 报告导出 PDF / Word **【规划中，未实现】**
- 测试脚本纳入版本管理

### 中期规划（尚未实现）

- 账号体系 + 历史报告库（需数据库）
- 个人跨文化能力成长曲线（多次诊断 → 趋势）
- 教师端：班级任务发布 + 批量批改视图
- 案例库众包扩充与质量审核流程

### 长期设想（尚未实现）

- 多语种扩展（英 → 日/韩/西/阿拉伯语场景）
- 与跨境电商平台/CRM 的轻量集成（邮件撰写时实时提示）
- 面向企业的跨文化沟通风险培训产品化

---

## 二十二、最终输出

### A. 100 字摘要

CrossBridge AI 面向跨境电商与国际商务沟通，检测英文沟通文本中的语言、商务礼仪与跨文化风险。
系统从语言准确性、商务礼貌、文化适配、表达清晰、合作友好五维打分，判定风险等级，
给出问题分析、优化表达与文化解释。已上线 6 页面 6 接口的完整 Demo，并完成 53 份用户反馈验证。

### B. 500 字上下文

**项目定位**：这是一个"AI + 商务英语 + 跨文化交际"的新文科实践创新项目，
**不是**英语学习软件、**不是**翻译软件、**不是**普通 AI 写作助手。
它只解决一件事：判断一段商务沟通文本在目标文化语境下是否存在风险，风险多大，应当怎么改。

**用户与场景**：面向跨境电商运营、外贸业务员与国际商务专业学生，
覆盖商务邮件、商务谈判、国际会议、客户沟通、日常交流、跨境电商、商务接待等场景，
支持美国、欧洲、日本、东南亚、中东等目标文化选择。

**产品形态**：Web 应用，6 个页面（首页 / 风险报告 / 案例库 / 沟通诊断 / 优化对比 / 模型说明），
6 个 API，零构建前端（原生 HTML+CSS+JS），后端 FastAPI + Docker + Render 部署，线上可直接访问。

**核心技术**：大模型 + 规则引擎双引擎。配置 DeepSeek Key 时走大模型，
两次结构化调用分别产出"场景与问题清单"和"评分与优化表达"；
无 Key 或调用异常时自动降级到基于 52 条案例知识池的规则引擎，并在结果中标注降级，
保证任何环境下演示不中断。评分为确定性算法：五维 0–10 分按 3/2/2/2/1 加权求和得 0–100 总分，
再映射为低/中/中高/高风险四级。

**数据资产**：30 条跨文化风险案例、22 条场景测试用例、8 组优化前后对比、30 条解决方案，
全部以 JSON 存放，可被非技术人员直接维护；系统不使用数据库，不持久化用户输入与报告。

**实践验证**：完成一轮 53 份用户反馈，92.5% 认为能帮助发现潜在问题，
94.3% 认为优化建议有参考价值，98.1% 认为跨文化分析提升了表达针对性，84.9% 认为可用于实际商务场景。

**已知限制**：无账号与历史记录、无多模态输入、接口串行导致诊断/对比页存在超时风险（P0 待修）、
案例库覆盖的国别与文化仍有限。

### C. 完整主档案

即本文件全部内容（第一至第二十一章 + 附录）。

---

## 附录一、当前已确认事实

1. 代码仓库 `https://github.com/Snape001S/CrossBridge-AI`（main 分支），当前提交 `b3e2302`（2026-08-30），首提交日期 2026-08-29。
2. 线上地址 `https://crossbridge-ai.onrender.com/` 可访问；`/api/health` 返回
   `status=ok, service=CrossBridge AI, version=1.0.0, ai_provider=deepseek, deepseek_configured=true`。
3. 线上接口实测（2026-09-04）：`analyze` 9.2s（`mode=ai`、`provider=deepseek`、`total=44`、`issues=3`、`strategies=3`、`related_cases=2`）；
   `health` 1.2–1.6s；`comparisons` 首次 118.0s / 缓存后 1.0s（`total=8`）；`diagnosis`（5 条）39.1s（`total=43`）。
4. 项目根目录结构：`backend/`、`data/`、`docs/`、`frontend/`、`scripts/`、`Dockerfile`、`render.yaml`、`LICENSE`(MIT)、`README.md`、`.gitignore`、`.dockerignore`。
5. 前端 6 个页面、后端 6 个接口，接口路径与 `main.py` 一致。
6. 五维维度名与权重：语言准确性 3.0 / 商务礼貌 2.0 / 文化适配 2.0 / 表达清晰 2.0 / 合作友好 1.0；总分 = 加权和（0–100）。
7. 风险阈值：≥85 低风险、≥70 中风险、≥50 中高风险、<50 高风险。
8. 配置项：`AI_PROVIDER` 默认 `auto`，`DEEPSEEK_MODEL` 默认 `deepseek-chat`，`REQUEST_TIMEOUT=90`，`MAX_LLM_RETRY=1`，HTTP 重试码 `{429,500,502,503,504}`，退避 1.5s/3s。
9. 数据条目数：`cross_culture_cases.json` 30、`scene_test_cases.json` 22、`comparison_pairs.json` 8、`case_resolutions.json` 30；**无数据库文件**。
10. 案例总数 52（30 + 22）即规则引擎知识池规模。
11. 前端超时：分析 25s、诊断 30s、对比 15s、案例 10s；离线兜底数据在 `mockdata.js`，会话传递用 `sessionStorage`（`cb_report_v1` / `cb_mode_v1`）。
12. `scripts/` 已被 `.gitignore`（第 24 行）排除，验收脚本 `acceptance_test.py`（21 项检查）**不在仓库中**。
13. DeepSeek 密钥仅存于 Render 环境变量，前端与仓库中无私钥；`/api/health` 仅暴露布尔值。
14. 团队与指导教师信息、高校、填报日期（2026-09-04）如第一章所载。
15. 用户反馈为唯一一轮 53 份：92.5% / 94.3% / 98.1% / 84.9%。
16. 桌面报告文件清单与字数、数据一致性修订（53 份、92.5%/94.3%/98.1%/84.9%）如第十七章所载。
17. 移动端 375/390/414/768 与桌面端 1024/1280/1440 已完成一轮布局核验与根因修复。
18. 池外边界的线上复测结果（2026-09-04，DeepSeek 模式）：
    `Just give me a refund.` → 中高风险（57 分）；`Your offer is not competitive.` → 高风险（47 分）；
    两条在原规则引擎下曾被判为"未检出风险"。
19. `analyzer._llm_analyze()` 以模型识别场景优先于用户所选场景（实测用户选 `ecommerce` → 返回 `customer`）。
20. 桌面旧口径报告文件共 3 个，已于 2026-09-04 **移动**（非删除）至
    `C:\Users\sjhjm\Desktop\报告旧版本存档（含26份旧数据-请勿提交）\`，同目录附 `说明-请先阅读.txt`；
    归档后 Python 严格复核：桌面根目录 10 个 docx 中含旧口径（26份/92.3%/88.5%/96.2%）的为 **0 个**。

## 附录二、当前待确认信息（需人工核对，勿当结论使用）

1. **比赛组别**未填写；**实践形式**勾选项需与最终提交版一致。
2. **答辩 PPT** 尚未制作（项目与桌面均无 `.pptx`）。
3. Render 环境变量完整清单与 `DEEPSEEK_MODEL` 实际取值是否被覆盖。
4. 重接口首次测量时约 30.9s 被服务端断连（`Remote end closed connection without response`）的**成因**
   （Render 冷启动 / 连接层限制 / 其他），以及是否需要预热请求规避。
5. 用户在前端选择的沟通场景被模型自动识别结果覆盖（`scene_final = data1.get("scene", scene)`）
   是否为有意设计，还是应当以用户选择为准。
6. 试用反馈的**回收对象与回收方式**（课程/班级/实习单位/线上渠道）与**教师评价意见**的具体内容与人数。
7. 报告 4-2 提到的"2026 年 8 月 30 日启动实践、9 月 1 日形成可运行版本"与 git 首提交
   （2026-08-29）之间的日期口径是否需要统一。
8. 是否将 `scripts/` 测试脚本、`README.html/.txt`、`打开Demo.bat` 纳入版本管理。
9. `附件1大学生新文科实践创新大赛实践创新告.docx` 文件名漏"报"字，
   是否改名为 `附件1大学生新文科实践创新大赛实践创新报告.docx`（另：该文件当前是否已填写组别/实践形式勾选项）。
10. 售后服务 / 商务谈判两个快速体验示例是否替换为更贴近跨境电商的文本。

## 附录三、未来规划清单（**均不代表已经实现**）

- 并发化 AI 调用 + 启动预热缓存，修复诊断/对比页超时（P0）
- 报告页直达引导输入、加载态与进度提示优化
- 图片/截图 OCR 输入、语音输入等多模态能力
- 报告导出 PDF / Word、批量导出
- 账号体系、历史报告库、跨文化能力成长曲线
- 教师端班级任务与批量批改视图
- 案例库众包扩充与审核流程
- 多语种扩展（日/韩/西/阿）、电商平台与 CRM 轻量集成、企业培训产品化
- 测试脚本版本化、CI 自动验收

---

## 附录四、凭据与安全约定

- **绝不**将 `DEEPSEEK_API_KEY`、`SILICONFLOW_API_KEY`、GitHub PAT 写入前端代码、仓库文件或本档案。
- 密钥只允许存在于：Render 环境变量、本地 `.env`（已被 `.gitignore` 忽略）。
- 推送使用临时 PAT 流程，用后立即重置 remote URL 并删除临时文件。
- 若发现密钥疑似泄露：立即在对应平台吊销并重新签发，再检查 git 历史。

## 附录五、维护规则（给后续协作者 / AI 助手）

1. **本文件是主档来源**。若代码与本文档冲突，**以代码为准**，并同步修正本文档。
2. 修改前先读本文件第一章至第八章，确认不越过第四章的"不做什么"边界。
3. 涉及评分、权重、风险阈值、接口契约的改动，必须同时更新第十章、第十三章。
4. 涉及比赛材料数字的改动，必须与"53 份"这一唯一真实数据集保持一致。
5. 每次代码改动的验收流程：运行 `python scripts/acceptance_test.py`（21 项检查）→
   移动端 375/390/414 与桌面端 1024/1280/1440 布局核验 → 推送（经本地代理 + PAT）→ 等待 Render 自动部署 → 线上健康检查。
6. 文档中**不得出现**任何未经核实的百分比、样本量或"广泛验证"类表述。

---

*本档案由项目实际代码、线上服务与本地文件核查生成；标注 **【待确认】** 的内容尚未验证，不作为结论使用。*
